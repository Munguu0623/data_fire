# API нууц түлхүүр — /predict болон /admin/reload endpoint-д шаардагдана
# Өөр нэр үүсгэх: python -c "import secrets; print(secrets.token_hex(32))"
API_KEY=change-me-to-a-random-secret

# CORS — таслалаар тусгаарласан хаягуудыг зөвшөөрнө (* = бүгдийг)
CORS_ORIGINS=*
